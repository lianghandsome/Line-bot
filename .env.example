# 複製這個檔案成 .env 並填入你自己的值（.env 不會被 git 追蹤）

# LINE Developers > Messaging API 頻道設定裡拿
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# PostgreSQL 連線字串（例如 Render 提供的 External Database URL）
# 本機開發若留空，會自動改用 accounting.db（SQLite），不需要另外裝資料庫
DATABASE_URL=
